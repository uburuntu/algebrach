services:
  algebrach:
    container_name: algebrach
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - app/.env.prod
    environment:
      IS_DOCKER: "True"
    restart: unless-stopped
